@model IEnumerable<InfoWeb>

@{
    ViewBag.Title = "Thông Tin Chung Của Web";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<table class="table table-middle">
    <tr>
        <th class="text-center">
            @Html.DisplayNameFor(model => model.Logo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WebName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Zalo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>

        <th></th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td class="text-center">
            <img src="~/images/@item.Logo"  width="100" height="auto"/>
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WebName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Phone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Zalo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>

        <td>
            @Html.ActionLink("Sửa", "Edit", new { id = item.Id }, new { @class = "btn btn-info btn-sm" })
            
        </td>
        <td>@Html.ActionLink("Xem thêm", "Details", new { id = item.Id }, new { @class = "btn btn-primary btn-sm" })</td>
    </tr>
}

</table>
